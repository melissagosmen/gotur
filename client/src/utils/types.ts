export type Item = {
    title: string;
    description: string;
    tag: Array<string>;
    images: Array<string>;
    publisher: string;
    comments: Array<string>;
    location: string;
    created: string;
}